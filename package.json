{
  "name": "sejf-private-ai-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check server.mjs && node --check public/app.js && node --check public/portal.js && npm test",
    "qa": "node scripts/responsive-qa.mjs"
  },
  "dependencies": {
    "animejs": "^4.1.3"
  },
  "devDependencies": {
    "puppeteer-core": "^24.16.0"
  }
}
